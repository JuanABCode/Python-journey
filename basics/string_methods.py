# string methods

name = input("Enter your full name: ")
phone_number = input("Enter your number: ")

# finds the length and counts spaces
# result = len(name)

# finds the first occurence of something
# result = name.find(" ")

# finds the last occurence of something meaning reverse
# result = name.rfind("r")

# name = name.capitalize()

# name = name.upper()

# name = name.lower()

# returns as a boolean and only true when only digits not spaces
# result = name.isdigit()

# only letter no numbers or spaces
# result = name.isalpha()

# counts how many times that thing is being said
# result = phone_number.count("-")

# replaces one thing to others
# result = phone_number.replace("-", " ")

# print(result)
